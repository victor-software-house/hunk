---
---

Break the landing page's feature grid into media-led sections captured from the real TUI — a retina review-stream screenshot plus looping clips of mouse support, split/stack layouts, and theme switching — with a reproducible capture script (`website/scripts/capture-media.ts`). Website-only change with no published package bump.
