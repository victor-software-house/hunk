# Downstream Patches

> Generated by `forkctl`; edit the manifest, not this file.

Base: `1306bd89f24f016ff4bde08abd94693bab2db974` (`1306bd89f24f016ff4bde08abd94693bab2db974`)

| Order | Patch | Kind | Purpose | Upstream status | Drop condition |
|--:|:--|:--|:--|:--|:--|
| 1 | `review-range-api` | source | Expose bounded review history and range replacement through the extension contract. | not-submitted | Upstream provides equivalent bounded history and review-range replacement APIs. |
| 2 | `multi-project-reviews` | source | Host independent named project reviews across the daemon, agent surface, and terminal UI. | not-submitted: downstream multi-project review workflow | Upstream supports equivalent independent named project reviews across TUI and agent control. |
| 3 | `files-history-navigator` | source | Add a collapsible Files tree and bounded local History range navigator to the built-in sidebar. | not-submitted | Upstream provides equivalent collapsible file navigation and two-endpoint local history selection. |
| 4 | `vsh-distribution` | tooling | Own VSH package identity, GitHub Packages native distribution, release validation, and pinned repository tooling. | inappropriate: downstream package identity and release ownership | The VSH Hunk fork and its scoped distribution are retired. |
| 5 | `fork-tooling` | tooling | Own fork policy, generated evidence, and integration configuration. | inappropriate: downstream-only fork maintenance | The downstream fork is retired. |

## Disabled

| Patch | Former commit | Original position | Reason |
|:--|:--|--:|:--|
| None | — | — | — |

## Checks

| Patch | Check | Applied at | Files | Command |
|:--|:--|:--|:--|:--|
| `vsh-distribution` | `stale-extension-import` | stack | `src/**/*.ts, src/**/*.tsx, scripts/**/*.ts, test/**/*.ts, examples/**/*.ts, examples/**/*.tsx, packages/**/*.ts` | `! grep -n 'from "hunkdiff/extension"' {files}` |

## History

| Event | Patch | Former commit | Target | Purpose |
|:--|:--|:--|:--|:--|
| None | — | — | — | — |
